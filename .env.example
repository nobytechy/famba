# Famba Fleet — frontend env. Copy to .env (local) or set in Netlify.
# Leave blank to run on the bundled demo data (the link still works).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON=

# FastAPI backend base URL (Render). Blank = use the built-in local insights.
VITE_API_BASE=
